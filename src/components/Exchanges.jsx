import React from 'react'
import { useGetCryptoExchangesQuery } from '../services/cryptoApi'

const Exchanges = () => {
  // const { data: exchanges, isFetching} = useGetCryptoExchangesQuery(coinId)
  // console.log(exchanges);

  return (
    <div>Exchanges</div>
  )
}

export default Exchanges